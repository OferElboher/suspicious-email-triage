# CRA reads REACT_APP_* at npm start / build time.
# Dev API calls use same-origin paths + setupProxy.js → port 3000 (never set REACT_APP_API_URL here).
REACT_APP_DEPLOYMENT_ENV=dev
REACT_APP_DJANGO_ADMIN_URL=http://localhost:8000/admin/
