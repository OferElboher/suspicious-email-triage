# `frontend/src/` — React application source

## Main areas

- `TriageApp.jsx` — primary UI shell and navigation.
- `views/` — larger screens (analytics charts, simulation panel).
- `hooks/` — reusable React hooks (polling).
- `api/` — small fetch helpers for the backend.
- `styles/` — global UI styling for the triage theme.
