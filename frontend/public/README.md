# `frontend/public/` — static assets

Contains static files served as-is by Create React App (favicon, manifest, etc.).

## Notes

- This folder is not meant for secrets or environment-specific credentials.
