# `shared/` — cross-package constants

This folder holds small pieces of configuration shared between backend and frontend to prevent accidental drift (for example pagination page sizes).

## Subfolders

- `config/` — shared constants (`pagination.js`).
