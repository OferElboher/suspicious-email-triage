triage_dbt_demo:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      user: "{{ env_var('POSTGRES_USER', 'triage') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'triage') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      dbname: "{{ env_var('POSTGRES_DB', 'triage_stats') }}"
      schema: public
      threads: 1
