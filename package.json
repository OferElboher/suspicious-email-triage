{
  "name": "suspicious-email-triage",
  "private": true,
  "description": "Monorepo: Node API, React UI, Kafka + Celery AI pipeline",
  "scripts": {
    "prepare": "husky",
    "setup:dev": "bash scripts/setup-local-dev.sh",
    "lint": "sh scripts/lint-all.sh",
    "test": "sh scripts/test-all.sh"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  }
}
