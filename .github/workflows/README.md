# `.github/workflows/` — CI workflows

## Files

- `ci.yml` — repository CI configuration (may include legacy/template steps; compare with `docs/ci_guide_node_python_django_legacy.md` when debugging).
