# `backend/core/tests/` — Django core tests

This folder contains tests for the Django `core` app.

## Running

Run these through the Django test command when the Django dependencies and settings are active. In normal local Node/React work, the main test entry point remains the repo-level `scripts/test-all.sh`.
