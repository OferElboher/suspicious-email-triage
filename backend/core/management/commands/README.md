# `backend/core/management/commands/` — Django CLI commands

This folder contains custom Django command modules.

## Files

- `run_kafka_consumer.py` — runs the Django-side Kafka consumer loop when that path is being exercised.
