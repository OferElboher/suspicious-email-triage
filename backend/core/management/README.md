# `backend/core/management/` — Django management command package

This folder follows Django’s standard layout for custom management commands.

## Subfolders

- `commands/` — command implementations that can be run with `python manage.py <command>`.
