# `backend/health/tests/` — Django health tests

This folder tests the Django health endpoint.

The tests are small by design: they provide confidence that the Django project can boot and answer a basic request when that stack is enabled.
