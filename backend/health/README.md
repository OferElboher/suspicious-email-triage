# `backend/health/` — Django health app

This folder contains a small Django app for health-check style endpoints.

## Files

- `views.py` and `urls.py` expose a simple health response.
- `tests/` contains tests for that endpoint.
- `apps.py`, `models.py`, and `admin.py` are standard Django app files.
