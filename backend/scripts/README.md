# `backend/scripts/` — backend utility scripts

This folder contains small scripts that help validate or operate backend configuration.

## Files

- `check_settings.py` — checks Django settings for the selected environment. It is mainly useful when the Django sidecar tree is being validated in CI or during migration work.
