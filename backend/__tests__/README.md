# `backend/__tests__/` — Jest tests

This folder contains automated tests for backend utilities.

## Notes

- Tests run in Node with Jest (`npm test`).
- Prefer deterministic unit tests here; integration tests may require dockerized dependencies.
