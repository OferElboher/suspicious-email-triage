# `infra/` — infrastructure definitions

Contains infrastructure-as-code style assets for local and CI environments.

## Subfolders

- `docker/` — `docker-compose.yml` and related runtime wiring for local stacks.
