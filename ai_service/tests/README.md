# `ai_service/tests/` — pytest suite

Unit tests for scoring helpers and other deterministic logic.

## Running

From repo root:

```bash
PYTHONPATH=ai_service "$(bash scripts/ensure-ai-service-venv.sh)" -m pytest -q ai_service/tests
```
